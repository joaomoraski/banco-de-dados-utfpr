1) select f.termo from FLASHCARD f inner join DECK_FLASHCARD df on f.id_flashcard = df.id_flashcard where df.nome_deck in (select utd.nome_deck from USUARIO_TEM_DECK utd);

2) select u.nome from USUARIO u inner join USUARIO_TEM_DECK utd on utd.id_usuario = u.id_usuario inner join DECK_FLASHCARD df on df.nome_deck = utd.nome_deck inner join FLASHCARD f on f.id_flashcard = df.id_flashcard where f.termo = "Zeus";

3) select distinct f.termo, count(*) from FLASHCARD f, DECK_FLASHCARD df where f.id_flashcard = df.id_flashcard GROUP BY f.id_flashcard;

4) select * from FLASHCARD  where id_flashcard not in (select id_flashcard from DECK_FLASHCARD);

5) select * from USUARIO u where 3 < (select count(*) from USUARIO_TEM_DECK utd where u.id_usuario = utd.id_usuario);
6) Enviar resposta em anexo.